package com.fangzuo.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.fangzuo.assist.Dao.Bibie;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BIBIE".
*/
public class BibieDao extends AbstractDao<Bibie, Void> {

    public static final String TABLENAME = "BIBIE";

    /**
     * Properties of entity Bibie.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property FCurrencyID = new Property(0, String.class, "FCurrencyID", false, "FCURRENCY_ID");
        public final static Property FNumber = new Property(1, String.class, "FNumber", false, "FNUMBER");
        public final static Property FName = new Property(2, String.class, "FName", false, "FNAME");
        public final static Property FExChangeRate = new Property(3, String.class, "FExChangeRate", false, "FEX_CHANGE_RATE");
        public final static Property FClassTypeId = new Property(4, String.class, "fClassTypeId", false, "F_CLASS_TYPE_ID");
    }


    public BibieDao(DaoConfig config) {
        super(config);
    }
    
    public BibieDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"BIBIE\" (" + //
                "\"FCURRENCY_ID\" TEXT," + // 0: FCurrencyID
                "\"FNUMBER\" TEXT," + // 1: FNumber
                "\"FNAME\" TEXT," + // 2: FName
                "\"FEX_CHANGE_RATE\" TEXT," + // 3: FExChangeRate
                "\"F_CLASS_TYPE_ID\" TEXT);"); // 4: fClassTypeId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"BIBIE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Bibie entity) {
        stmt.clearBindings();
 
        String FCurrencyID = entity.getFCurrencyID();
        if (FCurrencyID != null) {
            stmt.bindString(1, FCurrencyID);
        }
 
        String FNumber = entity.getFNumber();
        if (FNumber != null) {
            stmt.bindString(2, FNumber);
        }
 
        String FName = entity.getFName();
        if (FName != null) {
            stmt.bindString(3, FName);
        }
 
        String FExChangeRate = entity.getFExChangeRate();
        if (FExChangeRate != null) {
            stmt.bindString(4, FExChangeRate);
        }
 
        String fClassTypeId = entity.getFClassTypeId();
        if (fClassTypeId != null) {
            stmt.bindString(5, fClassTypeId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Bibie entity) {
        stmt.clearBindings();
 
        String FCurrencyID = entity.getFCurrencyID();
        if (FCurrencyID != null) {
            stmt.bindString(1, FCurrencyID);
        }
 
        String FNumber = entity.getFNumber();
        if (FNumber != null) {
            stmt.bindString(2, FNumber);
        }
 
        String FName = entity.getFName();
        if (FName != null) {
            stmt.bindString(3, FName);
        }
 
        String FExChangeRate = entity.getFExChangeRate();
        if (FExChangeRate != null) {
            stmt.bindString(4, FExChangeRate);
        }
 
        String fClassTypeId = entity.getFClassTypeId();
        if (fClassTypeId != null) {
            stmt.bindString(5, fClassTypeId);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public Bibie readEntity(Cursor cursor, int offset) {
        Bibie entity = new Bibie( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // FCurrencyID
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // FNumber
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // FName
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // FExChangeRate
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // fClassTypeId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Bibie entity, int offset) {
        entity.setFCurrencyID(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setFNumber(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setFName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setFExChangeRate(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setFClassTypeId(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(Bibie entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(Bibie entity) {
        return null;
    }

    @Override
    public boolean hasKey(Bibie entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
