package com.fangzuo.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.fangzuo.assist.Dao.Unit;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "UNIT".
*/
public class UnitDao extends AbstractDao<Unit, Void> {

    public static final String TABLENAME = "UNIT";

    /**
     * Properties of entity Unit.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property FMeasureUnitID = new Property(0, String.class, "FMeasureUnitID", false, "FMEASURE_UNIT_ID");
        public final static Property FUnitGroupID = new Property(1, String.class, "FUnitGroupID", false, "FUNIT_GROUP_ID");
        public final static Property FNumber = new Property(2, String.class, "FNumber", false, "FNUMBER");
        public final static Property FName = new Property(3, String.class, "FName", false, "FNAME");
        public final static Property FCoefficient = new Property(4, String.class, "FCoefficient", false, "FCOEFFICIENT");
    }


    public UnitDao(DaoConfig config) {
        super(config);
    }
    
    public UnitDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"UNIT\" (" + //
                "\"FMEASURE_UNIT_ID\" TEXT," + // 0: FMeasureUnitID
                "\"FUNIT_GROUP_ID\" TEXT," + // 1: FUnitGroupID
                "\"FNUMBER\" TEXT," + // 2: FNumber
                "\"FNAME\" TEXT," + // 3: FName
                "\"FCOEFFICIENT\" TEXT);"); // 4: FCoefficient
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"UNIT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Unit entity) {
        stmt.clearBindings();
 
        String FMeasureUnitID = entity.getFMeasureUnitID();
        if (FMeasureUnitID != null) {
            stmt.bindString(1, FMeasureUnitID);
        }
 
        String FUnitGroupID = entity.getFUnitGroupID();
        if (FUnitGroupID != null) {
            stmt.bindString(2, FUnitGroupID);
        }
 
        String FNumber = entity.getFNumber();
        if (FNumber != null) {
            stmt.bindString(3, FNumber);
        }
 
        String FName = entity.getFName();
        if (FName != null) {
            stmt.bindString(4, FName);
        }
 
        String FCoefficient = entity.getFCoefficient();
        if (FCoefficient != null) {
            stmt.bindString(5, FCoefficient);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Unit entity) {
        stmt.clearBindings();
 
        String FMeasureUnitID = entity.getFMeasureUnitID();
        if (FMeasureUnitID != null) {
            stmt.bindString(1, FMeasureUnitID);
        }
 
        String FUnitGroupID = entity.getFUnitGroupID();
        if (FUnitGroupID != null) {
            stmt.bindString(2, FUnitGroupID);
        }
 
        String FNumber = entity.getFNumber();
        if (FNumber != null) {
            stmt.bindString(3, FNumber);
        }
 
        String FName = entity.getFName();
        if (FName != null) {
            stmt.bindString(4, FName);
        }
 
        String FCoefficient = entity.getFCoefficient();
        if (FCoefficient != null) {
            stmt.bindString(5, FCoefficient);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public Unit readEntity(Cursor cursor, int offset) {
        Unit entity = new Unit( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // FMeasureUnitID
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // FUnitGroupID
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // FNumber
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // FName
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // FCoefficient
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Unit entity, int offset) {
        entity.setFMeasureUnitID(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setFUnitGroupID(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setFNumber(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setFName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setFCoefficient(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(Unit entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(Unit entity) {
        return null;
    }

    @Override
    public boolean hasKey(Unit entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
