package com.fangzuo.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.fangzuo.assist.Dao.GetGoodsDepartment;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "GET_GOODS_DEPARTMENT".
*/
public class GetGoodsDepartmentDao extends AbstractDao<GetGoodsDepartment, Void> {

    public static final String TABLENAME = "GET_GOODS_DEPARTMENT";

    /**
     * Properties of entity GetGoodsDepartment.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property FItemID = new Property(0, String.class, "FItemID", false, "FITEM_ID");
        public final static Property FDeleted = new Property(1, String.class, "FDeleted", false, "FDELETED");
        public final static Property FNumber = new Property(2, String.class, "FNumber", false, "FNUMBER");
        public final static Property FName = new Property(3, String.class, "FName", false, "FNAME");
        public final static Property FDetail = new Property(4, String.class, "FDetail", false, "FDETAIL");
    }


    public GetGoodsDepartmentDao(DaoConfig config) {
        super(config);
    }
    
    public GetGoodsDepartmentDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"GET_GOODS_DEPARTMENT\" (" + //
                "\"FITEM_ID\" TEXT," + // 0: FItemID
                "\"FDELETED\" TEXT," + // 1: FDeleted
                "\"FNUMBER\" TEXT," + // 2: FNumber
                "\"FNAME\" TEXT," + // 3: FName
                "\"FDETAIL\" TEXT);"); // 4: FDetail
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"GET_GOODS_DEPARTMENT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, GetGoodsDepartment entity) {
        stmt.clearBindings();
 
        String FItemID = entity.getFItemID();
        if (FItemID != null) {
            stmt.bindString(1, FItemID);
        }
 
        String FDeleted = entity.getFDeleted();
        if (FDeleted != null) {
            stmt.bindString(2, FDeleted);
        }
 
        String FNumber = entity.getFNumber();
        if (FNumber != null) {
            stmt.bindString(3, FNumber);
        }
 
        String FName = entity.getFName();
        if (FName != null) {
            stmt.bindString(4, FName);
        }
 
        String FDetail = entity.getFDetail();
        if (FDetail != null) {
            stmt.bindString(5, FDetail);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, GetGoodsDepartment entity) {
        stmt.clearBindings();
 
        String FItemID = entity.getFItemID();
        if (FItemID != null) {
            stmt.bindString(1, FItemID);
        }
 
        String FDeleted = entity.getFDeleted();
        if (FDeleted != null) {
            stmt.bindString(2, FDeleted);
        }
 
        String FNumber = entity.getFNumber();
        if (FNumber != null) {
            stmt.bindString(3, FNumber);
        }
 
        String FName = entity.getFName();
        if (FName != null) {
            stmt.bindString(4, FName);
        }
 
        String FDetail = entity.getFDetail();
        if (FDetail != null) {
            stmt.bindString(5, FDetail);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public GetGoodsDepartment readEntity(Cursor cursor, int offset) {
        GetGoodsDepartment entity = new GetGoodsDepartment( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // FItemID
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // FDeleted
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // FNumber
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // FName
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // FDetail
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, GetGoodsDepartment entity, int offset) {
        entity.setFItemID(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setFDeleted(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setFNumber(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setFName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setFDetail(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(GetGoodsDepartment entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(GetGoodsDepartment entity) {
        return null;
    }

    @Override
    public boolean hasKey(GetGoodsDepartment entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
