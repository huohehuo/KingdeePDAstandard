package com.fangzuo.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.fangzuo.assist.Dao.Wanglaikemu;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "WANGLAIKEMU".
*/
public class WanglaikemuDao extends AbstractDao<Wanglaikemu, Void> {

    public static final String TABLENAME = "WANGLAIKEMU";

    /**
     * Properties of entity Wanglaikemu.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property FAccountID = new Property(0, String.class, "FAccountID", false, "FACCOUNT_ID");
        public final static Property FNumber = new Property(1, String.class, "FNumber", false, "FNUMBER");
        public final static Property FFullName = new Property(2, String.class, "FFullName", false, "FFULL_NAME");
    }


    public WanglaikemuDao(DaoConfig config) {
        super(config);
    }
    
    public WanglaikemuDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"WANGLAIKEMU\" (" + //
                "\"FACCOUNT_ID\" TEXT," + // 0: FAccountID
                "\"FNUMBER\" TEXT," + // 1: FNumber
                "\"FFULL_NAME\" TEXT);"); // 2: FFullName
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"WANGLAIKEMU\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Wanglaikemu entity) {
        stmt.clearBindings();
 
        String FAccountID = entity.getFAccountID();
        if (FAccountID != null) {
            stmt.bindString(1, FAccountID);
        }
 
        String FNumber = entity.getFNumber();
        if (FNumber != null) {
            stmt.bindString(2, FNumber);
        }
 
        String FFullName = entity.getFFullName();
        if (FFullName != null) {
            stmt.bindString(3, FFullName);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Wanglaikemu entity) {
        stmt.clearBindings();
 
        String FAccountID = entity.getFAccountID();
        if (FAccountID != null) {
            stmt.bindString(1, FAccountID);
        }
 
        String FNumber = entity.getFNumber();
        if (FNumber != null) {
            stmt.bindString(2, FNumber);
        }
 
        String FFullName = entity.getFFullName();
        if (FFullName != null) {
            stmt.bindString(3, FFullName);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public Wanglaikemu readEntity(Cursor cursor, int offset) {
        Wanglaikemu entity = new Wanglaikemu( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // FAccountID
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // FNumber
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // FFullName
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Wanglaikemu entity, int offset) {
        entity.setFAccountID(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setFNumber(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setFFullName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(Wanglaikemu entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(Wanglaikemu entity) {
        return null;
    }

    @Override
    public boolean hasKey(Wanglaikemu entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
